# Base de données PostgreSQL
# Format: postgresql://user:password@host:port/database
DATABASE_URL="postgresql://ebe:ebe_password@localhost:5432/ebe_payload"

# Payload CMS
# Générer une clé aléatoire de 32+ caractères (ex: openssl rand -base64 32)
PAYLOAD_SECRET="change_me_long_random_secret_key_min_32_chars"

# URL du site
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Revalidation ISR
# Générer une clé aléatoire (ex: openssl rand -base64 32)
REVALIDATION_SECRET="change_me_too_revalidation_secret"

# Admin initial (créé automatiquement au seed si absent)
ADMIN_EMAIL="admin@ebeconsulting.fr"
ADMIN_PASSWORD="ChangeMeStrong123!"

